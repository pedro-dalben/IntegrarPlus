<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title><%= content_for(:title) || "Integrar Plus - Admin" %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= vite_client_tag %>
    <%= vite_stylesheet_tag "application" %>
    <%= vite_javascript_tag "entrypoints/application" %>
    <% if Rails.env.development? %>
      <script type="importmap">{"imports":{}}</script>
    <% end %>
  </head>

  <body class="bg-gray-50 dark:bg-gray-900"
        data-controller="session-timeout loading"
        data-session-timeout-timeout-minutes-value="<%= Rails.env.production? ? 60 : 60 %>"
        data-session-timeout-warning-minutes-value="0"
        data-session-timeout-logout-url-value="<%= destroy_user_session_path %>"
        data-session-timeout-is-production-value="<%= Rails.env.production? %>">

    <!-- Global Loading Spinner -->
    <div data-loading-target="spinner" class="hidden fixed inset-0 z-50 items-center justify-center bg-black bg-opacity-25">
      <%= render Ui::LoadingSpinnerComponent.new(size: :large, color: :white) %>
    </div>

    <!-- Skip Link para Acessibilidade -->
    <a href="#main-content" class="skip-link">Pular para o conteúdo principal</a>

    <!-- Layout Container -->
    <div class="flex h-screen overflow-hidden layout-container">

      <!-- Drawer mobile + fixa desktop -->
      <%= render Ui::SidebarComponent.new(current_path: request.path, current_user: current_user) %>

      <!-- Content Area -->
      <div class="flex flex-1 flex-col overflow-hidden content-area">

        <!-- Header -->
        <%= render Ui::HeaderComponent.new(user: current_user) %>

        <!-- Main Content -->
        <main id="main-content" class="flex-1 overflow-y-auto main-content">
          <div class="mx-auto w-full p-2 sm:p-4 lg:p-6">
            <!-- Breadcrumb -->
            <%= render Ui::BreadcrumbComponent.new(current_path: request.path) %>

            <!-- Flash Messages -->
            <% if notice %>
              <div class="mb-6 bg-teal-50 border border-teal-200 rounded-md p-4 dark:bg-teal-800/10 dark:border-teal-900 dark:text-teal-500">
                <div class="flex">
                  <div class="flex-shrink-0">
                    <svg class="size-4 text-teal-400 mt-0.5" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
                      <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zm-3.97-3.03a.75.75 0 0 0-1.08.022L7.477 9.417 5.384 7.323a.75.75 0 0 0-1.06 1.061L6.97 11.03a.75.75 0 0 0 1.079-.02l3.992-4.99a.75.75 0 0 0-.01-1.05z"/>
                    </svg>
                  </div>
                  <div class="ms-3">
                    <p class="text-sm text-teal-800 dark:text-teal-400">
                      <%= notice %>
                    </p>
                  </div>
                </div>
              </div>
            <% end %>

            <% if alert %>
              <div class="mb-6 bg-red-50 border border-red-200 rounded-md p-4 dark:bg-red-800/10 dark:border-red-900">
                <div class="flex">
                  <div class="flex-shrink-0">
                    <svg class="size-4 text-red-400 mt-0.5" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
                      <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zM5.354 4.646a.5.5 0 1 0-.708.708L7.293 8l-2.647 2.646a.5.5 0 0 0 .708.708L8 8.707l2.646 2.647a.5.5 0 0 0 .708-.708L8.707 8l2.647-2.646a.5.5 0 0 0-.708-.708L8 7.293 5.354 4.646z"/>
                    </svg>
                  </div>
                  <div class="ms-3">
                    <p class="text-sm text-red-800 dark:text-red-400">
                      <%= alert %>
                    </p>
                  </div>
                </div>
              </div>
            <% end %>

            <!-- Page Content -->
            <%= yield %>
          </div>
        </main>
      </div>


    </div>

    <!-- Modal de Aviso de Timeout -->
    <div data-session-timeout-target="warning" class="hidden fixed inset-0 z-[9999] flex items-center justify-center p-4" aria-labelledby="modal-title" role="dialog" aria-modal="true">
      <!-- Backdrop -->
      <div class="fixed inset-0 bg-black bg-opacity-50 backdrop-blur-sm transition-opacity"></div>

      <!-- Modal -->
      <div class="relative bg-white rounded-2xl shadow-2xl max-w-md w-full mx-auto transform transition-all dark:bg-gray-800">
        <!-- Header -->
        <div class="flex items-center justify-center p-6 border-b border-gray-200 dark:border-gray-700">
          <div class="flex items-center justify-center w-16 h-16 bg-red-100 rounded-full dark:bg-red-900">
            <svg class="w-8 h-8 text-red-600 dark:text-red-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z" />
            </svg>
          </div>
        </div>

        <!-- Content -->
        <div class="p-6 text-center">
          <h3 class="text-xl font-semibold text-gray-900 dark:text-white mb-4" id="modal-title">
            Sessão Expirando
          </h3>

          <div class="space-y-3">
            <p class="text-gray-600 dark:text-gray-300">
              <strong>Você está inativo há muito tempo!</strong>
            </p>

            <div class="bg-red-50 border border-red-200 rounded-lg p-4 dark:bg-red-900/20 dark:border-red-800">
              <p class="text-sm text-red-800 dark:text-red-200">
                Você será deslogado automaticamente em
                <span data-session-timeout-target="countdown" class="font-bold text-red-600 dark:text-red-400 text-lg">30</span>
                segundos.
              </p>
            </div>

            <p class="text-sm text-gray-500 dark:text-gray-400">
              Clique em "Continuar" para manter sua sessão ativa.
            </p>
          </div>
        </div>

        <!-- Footer -->
        <div class="flex justify-center p-6 bg-gray-50 dark:bg-gray-700 rounded-b-2xl">
          <button type="button"
                  data-action="click->session-timeout#extendSession"
                  class="px-8 py-3 bg-blue-600 text-white font-medium rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors">
            Continuar
          </button>
        </div>
      </div>
    </div>
  </body>
</html>
