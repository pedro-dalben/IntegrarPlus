<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Integrar Plus - Admin" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= vite_client_tag %>
    <%= vite_javascript_tag "entrypoints/application" %>
  </head>

  <body class="h-full bg-gray-50 dark:bg-neutral-900" x-data="{ sidebarToggle: false, darkMode: false }">
    <!-- Layout Container -->
    <div class="bg-gray-50 dark:bg-neutral-900">
      
      <!-- Sidebar -->
      <%= render Ui::SidebarComponent.new(current_path: request.path) %>
      
      <!-- Header -->
      <%= render Ui::HeaderComponent.new(user: current_user) %>
      
      <!-- Sidebar Overlay for Mobile -->
      <div class="sidebar-overlay fixed inset-0 z-30 bg-gray-900/50 lg:hidden" 
           x-show="false" 
           x-transition:enter="transition-opacity ease-linear duration-300"
           x-transition:enter-start="opacity-0"
           x-transition:enter-end="opacity-100"
           x-transition:leave="transition-opacity ease-linear duration-300"
           x-transition:leave-start="opacity-100"
           x-transition:leave-end="opacity-0"
           @click="document.documentElement.classList.remove('sidebar-open')">
      </div>

      <!-- Main Content -->
      <div class="w-full lg:ps-64">
        <div class="p-4 sm:p-6 space-y-4 sm:space-y-6">
          
          <!-- Flash Messages -->
          <% if notice %>
            <div class="bg-teal-50 border border-teal-200 rounded-md p-4 dark:bg-teal-800/10 dark:border-teal-900 dark:text-teal-500">
              <div class="flex">
                <div class="flex-shrink-0">
                  <svg class="size-4 text-teal-400 mt-0.5" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
                    <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zm-3.97-3.03a.75.75 0 0 0-1.08.022L7.477 9.417 5.384 7.323a.75.75 0 0 0-1.06 1.061L6.97 11.03a.75.75 0 0 0 1.079-.02l3.992-4.99a.75.75 0 0 0-.01-1.05z"/>
                  </svg>
                </div>
                <div class="ms-3">
                  <p class="text-sm text-teal-800 dark:text-teal-400">
                    <%= notice %>
                  </p>
                </div>
              </div>
            </div>
          <% end %>

          <% if alert %>
            <div class="bg-red-50 border border-red-200 rounded-md p-4 dark:bg-red-800/10 dark:border-red-900">
              <div class="flex">
                <div class="flex-shrink-0">
                  <svg class="size-4 text-red-400 mt-0.5" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
                    <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zM5.354 4.646a.5.5 0 1 0-.708.708L7.293 8l-2.647 2.646a.5.5 0 0 0 .708.708L8 8.707l2.646 2.647a.5.5 0 0 0 .708-.708L8.707 8l2.647-2.646a.5.5 0 0 0-.708-.708L8 7.293 5.354 4.646z"/>
                  </svg>
                </div>
                <div class="ms-3">
                  <p class="text-sm text-red-800 dark:text-red-400">
                    <%= alert %>
                  </p>
                </div>
              </div>
            </div>
          <% end %>

          <!-- Page Content -->
          <%= yield %>
          
        </div>
      </div>
    </div>

    <!-- Alpine.js cloak style -->
    <style>
      [x-cloak] { display: none !important; }
    </style>
  </body>
</html>