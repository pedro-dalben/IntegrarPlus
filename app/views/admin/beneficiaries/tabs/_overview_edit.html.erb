<%= form_with model: [:admin, beneficiary], local: true do |form| %>
  <%= render Alerts::ErrorComponent.new(model: beneficiary, title: "Erro ao salvar beneficiário") %>

  <div class="space-y-6">
    <%= render partial: 'admin/beneficiaries/form', locals: { beneficiary: beneficiary, form: form } %>

    <div class="flex justify-end gap-3 pt-4 border-t border-gray-200 dark:border-gray-800">
      <button type="button"
              data-action="click->edit-mode#toggle"
              class="shadow-theme-xs inline-flex items-center justify-center gap-2 rounded-lg bg-white px-4 py-3 text-sm font-medium text-gray-700 ring-1 ring-gray-300 transition hover:bg-gray-50 dark:bg-gray-800 dark:text-gray-400 dark:ring-gray-700 dark:hover:bg-white/[0.03]">
        Cancelar
      </button>
      <%= form.submit "Salvar", class: "bg-brand-500 shadow-theme-xs hover:bg-brand-600 inline-flex items-center justify-center gap-2 rounded-lg px-4 py-3 text-sm font-medium text-white transition" %>
    </div>
  </div>
<% end %>
