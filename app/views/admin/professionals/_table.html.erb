<div class="custom-scrollbar overflow-x-auto">
  <table class="w-full table-auto">
    <thead>
      <tr class="border-b border-gray-200 dark:divide-gray-800 dark:border-gray-800">
        <th class="px-5 py-4 text-left text-xs font-medium text-gray-500 dark:text-gray-400">
          Profissional
        </th>
        <th class="px-5 py-4 text-left text-xs font-medium text-gray-500 dark:text-gray-400">
          Email
        </th>
        <th class="px-5 py-4 text-left text-xs font-medium text-gray-500 dark:text-gray-400">
          Status
        </th>
        <th class="px-5 py-4 text-left text-xs font-medium text-gray-500 dark:text-gray-400">
          Usuário
        </th>
        <th class="px-5 py-4 text-left text-xs font-medium text-gray-500 dark:text-gray-400">
          Criado em
        </th>
        <th class="px-5 py-4 text-left text-xs font-medium text-gray-500 dark:text-gray-400">
          <div class="relative">
            <span class="sr-only">Ações</span>
          </div>
        </th>
      </tr>
    </thead>
    <tbody class="divide-x divide-y divide-gray-200 dark:divide-gray-800" data-search-target="results">
      <% professionals.each do |professional| %>
        <tr class="transition hover:bg-gray-50 dark:hover:bg-gray-900"
            data-search-target="row">
          <td class="px-5 py-4 whitespace-nowrap">
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center dark:bg-blue-900/20">
                <svg class="w-5 h-5 text-blue-600 dark:text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                </svg>
              </div>
              <div>
                <h6 class="text-sm font-medium text-gray-800 dark:text-white">
                  <%= professional.full_name %>
                </h6>
              </div>
            </div>
          </td>
          <td class="px-5 py-4 whitespace-nowrap text-sm text-gray-600 dark:text-gray-400">
            <%= professional.email %>
          </td>
          <td class="px-5 py-4 whitespace-nowrap">
            <% if professional.active? %>
              <span class="text-theme-xs rounded-full bg-green-50 px-2 py-0.5 font-medium text-green-700 dark:bg-green-500/15 dark:text-green-500">
                Ativo
              </span>
            <% else %>
              <span class="text-theme-xs rounded-full bg-red-50 px-2 py-0.5 font-medium text-red-700 dark:bg-red-500/15 dark:text-red-500">
                Inativo
              </span>
            <% end %>
          </td>
          <td class="px-5 py-4 whitespace-nowrap">
            <% if professional.user %>
              <% if professional.user.confirmed_invite? %>
                <span class="text-theme-xs rounded-full bg-green-50 px-2 py-0.5 font-medium text-green-700 dark:bg-green-500/15 dark:text-green-500">
                  ✅ Ativo
                </span>
              <% elsif professional.user.pending_invite? %>
                <span class="text-theme-xs rounded-full bg-yellow-50 px-2 py-0.5 font-medium text-yellow-700 dark:bg-yellow-500/15 dark:text-yellow-500">
                  ⏳ Pendente
                </span>
              <% elsif professional.user.latest_invite&.expired? %>
                <span class="text-theme-xs rounded-full bg-red-50 px-2 py-0.5 font-medium text-red-700 dark:bg-red-500/15 dark:text-red-500">
                  🔄 Expirado
                </span>
              <% end %>
              <%= link_to 'Ver Usuário', admin_user_path(professional.user), class: 'text-blue-600 hover:text-blue-800 text-xs ml-1' %>
            <% else %>
              <span class="text-theme-xs rounded-full bg-gray-50 px-2 py-0.5 font-medium text-gray-700 dark:bg-gray-500/15 dark:text-gray-500">
                Sem usuário
              </span>
            <% end %>
          </td>
          <td class="px-5 py-4 whitespace-nowrap text-sm text-gray-600 dark:text-gray-400">
            <%= l(professional.created_at, format: :short) %>
          </td>
          <td class="px-5 py-4 whitespace-nowrap text-right text-sm font-medium">
            <div class="flex items-center justify-end gap-2">
              <%= link_to admin_professional_path(professional),
                  class: "text-brand-600 hover:text-brand-900 dark:text-brand-400 dark:hover:text-brand-300",
                  title: "Ver detalhes" do %>
                <i class="bi bi-eye"></i>
              <% end %>
              <%= link_to edit_admin_professional_path(professional),
                  class: "text-gray-600 hover:text-gray-900 dark:text-gray-400 dark:hover:text-gray-300",
                  title: "Editar" do %>
                <i class="bi bi-pencil"></i>
              <% end %>
              <%= link_to admin_professional_path(professional),
                  method: :delete,
                  data: {
                    confirm: "Tem certeza que deseja excluir este profissional?",
                    turbo_method: :delete
                  },
                  class: "text-red-600 hover:text-red-900 dark:text-red-400 dark:hover:text-red-300",
                  title: "Excluir" do %>
                <i class="bi bi-trash"></i>
              <% end %>
            </div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<!-- Paginação -->
<% if pagy.pages > 1 %>
  <div class="mt-6">
    <%= render Ui::PaginationComponent.new(pagy: pagy) %>
  </div>
<% end %>
