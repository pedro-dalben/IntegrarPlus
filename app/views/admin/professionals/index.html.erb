<div data-controller="search" 
     data-search-url-value="<%= admin_professionals_path %>"
     data-search-debounce-value="500">

<%= render Ui::DataTableComponent.new(searchable: true) do |component| %>
  <% component.with_actions do %>
    <%= link_to new_admin_professional_path, class: "bg-blue-600 hover:bg-blue-700 inline-flex items-center justify-center gap-2 rounded-lg px-4 py-2 text-sm font-medium text-white transition" do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/>
      </svg>
      Novo Profissional
    <% end %>
  <% end %>

  <% component.with_search do %>
    <input type="text" 
           placeholder="Buscar profissionais..." 
           class="h-11 w-full rounded-lg border border-gray-300 bg-white py-2.5 pr-4 pl-10 text-sm text-gray-800 placeholder:text-gray-400 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 dark:border-gray-700 dark:bg-gray-800 dark:text-white dark:placeholder:text-gray-400 sm:w-[320px]"
           data-search-target="input"
           data-action="input->search#search">
  <% end %>

  <%= render 'table', professionals: @professionals, pagy: @pagy %>
<% end %>
</div>
