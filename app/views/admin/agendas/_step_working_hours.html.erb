<div class="space-y-6" data-controller="agendas-working-hours">
  <div>
    <h2 class="text-lg font-medium text-gray-900 mb-4">Grade de Horários</h2>
    <p class="text-sm text-gray-600 mb-6">Configure os horários de funcionamento e duração dos slots</p>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
    <div class="lg:col-span-2">
      <div class="bg-white border border-gray-200 rounded-lg p-6">
        <h3 class="text-sm font-medium text-gray-900 mb-4">Configuração dos Slots</h3>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
          <div>
            <%= form.label :slot_duration_minutes, "Duração do Slot (minutos)", class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= form.number_field :slot_duration_minutes,
                  value: form.object.slot_duration_minutes || 50,
                  min: 1,
                  class: "w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500",
                  data: { action: "change->agendas-working-hours#updatePreview" } %>
          </div>

          <div>
            <%= form.label :buffer_minutes, "Buffer entre Slots (minutos)", class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= form.number_field :buffer_minutes,
                  value: form.object.buffer_minutes || 10,
                  min: 0,
                  class: "w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500",
                  data: { action: "change->agendas-working-hours#updatePreview" } %>
          </div>
        </div>

        <div data-agendas-working-hours-target="preview" class="mb-6 p-3 bg-blue-50 border border-blue-200 rounded-lg">
          <div class="flex items-center">
            <svg class="h-5 w-5 text-blue-400 mr-2" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
            </svg>
            <span class="text-sm text-blue-700">
              Isso gera slots de <span data-agendas-working-hours-target="duration">50</span>' + <span data-agendas-working-hours-target="buffer">10</span>' buffer
            </span>
          </div>
        </div>

        <h3 class="text-sm font-medium text-gray-900 mb-4">Horários por Dia da Semana</h3>

        <div class="space-y-4">
          <% Date::DAYNAMES.each_with_index do |day_name, wday| %>
            <div class="border border-gray-200 rounded-lg p-4" data-day="<%= wday %>">
              <div class="flex items-center justify-between mb-3">
                <div class="flex items-center">
                  <input type="checkbox"
                         class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded"
                         data-action="change->agendas-working-hours#toggleDay"
                         data-day="<%= wday %>"
                         id="day_<%= wday %>">
                  <label for="day_<%= wday %>" class="ml-2 text-sm font-medium text-gray-900">
                    <%= day_name %>
                  </label>
                </div>
                <button type="button"
                        class="ta-btn ta-btn-secondary text-sm"
                        data-action="click->agendas-working-hours#addPeriod"
                        data-day="<%= wday %>">
                  + Adicionar Período
                </button>
              </div>

              <div data-agendas-working-hours-target="periods" data-day="<%= wday %>" class="space-y-2">
                <!-- Períodos serão adicionados dinamicamente -->
              </div>
            </div>
          <% end %>
        </div>

        <div class="mt-4 flex gap-2">
          <button type="button"
                  class="ta-btn ta-btn-secondary"
                  data-action="click->agendas-working-hours#copyToAllDays">
            Copiar para Todos os Dias
          </button>
          <button type="button"
                  class="ta-btn ta-btn-primary"
                  data-action="click->agendas-working-hours#generatePreview">
            Gerar Prévia de Slots
          </button>
        </div>
      </div>
    </div>

    <div>
      <div class="bg-white border border-gray-200 rounded-lg p-6">
        <h3 class="text-sm font-medium text-gray-900 mb-4">Exceções</h3>
        <p class="text-sm text-gray-600 mb-4">Configure datas especiais ou feriados</p>

        <div data-agendas-working-hours-target="exceptions" class="space-y-2">
          <!-- Exceções serão adicionadas dinamicamente -->
        </div>

        <button type="button"
                class="ta-btn ta-btn-secondary w-full"
                data-action="click->agendas-working-hours#addException">
          + Adicionar Exceção
        </button>
      </div>

      <div class="mt-6 bg-green-50 border border-green-200 rounded-lg p-4">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
            </svg>
          </div>
          <div class="ml-3">
            <h3 class="text-sm font-medium text-green-800">Dicas</h3>
            <div class="mt-2 text-sm text-green-700">
              <ul class="list-disc list-inside space-y-1">
                <li>Marque os dias em que a agenda funcionará</li>
                <li>Adicione múltiplos períodos por dia se necessário</li>
                <li>Use exceções para feriados ou datas especiais</li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <%= form.hidden_field :working_hours, data: { agendas_working_hours_target: "workingHoursInput" } %>
</div>
