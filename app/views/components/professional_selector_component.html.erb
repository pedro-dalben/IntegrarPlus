<div class="space-y-6" data-controller="professional-selector" data-multiple="<%= multiple? %>">
  <div>
    <h2 class="text-lg font-medium text-gray-900 mb-4">Profissionais Vinculados</h2>
    <p class="text-sm text-gray-600 mb-6">Selecione os profissionais que podem atender nesta agenda</p>
  </div>

  <% if searchable %>
    <div class="bg-gray-50 rounded-lg p-4 mb-6">
      <div class="flex items-center justify-between">
        <div>
          <h3 class="text-sm font-medium text-gray-900">Buscar Profissionais</h3>
          <p class="text-sm text-gray-600">Digite para buscar e selecionar</p>
        </div>
        <button type="button" class="ta-btn ta-btn-primary">
          Selecionar Todos Aptos
        </button>
      </div>

      <div class="mt-3">
        <select multiple data-professional-selector-target="tomselect" data-url="/admin/agendas/search_professionals"></select>
      </div>
    </div>
  <% end %>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    

    <div>
      <h3 class="text-sm font-medium text-gray-900 mb-3">Profissionais Selecionados</h3>
      <div data-professional-selector-target="selectedProfessionalsContainer"
           class="border border-gray-200 rounded-lg min-h-32 p-3"></div>
    </div>
  </div>

  <div class="ta-alert ta-alert-warning">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-yellow-400" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
        </svg>
      </div>
      <div class="ml-3">
        <h3 class="text-sm font-medium text-yellow-800">Importante</h3>
        <div class="mt-2 text-sm text-yellow-700">
          <p>É necessário selecionar pelo menos um profissional para esta agenda.</p>
        </div>
      </div>
    </div>
  </div>

  <div data-professional-selector-target="hiddenInputs">
    <% selected_professionals.each do |prof| %>
      <input type="hidden"
             name="agenda[professional_ids][]"
             value="<%= prof.id %>"
             data-professional-id="<%= prof.id %>"
             data-professional-name="<%= prof.full_name %>"
             data-professional-specialties="<%= prof.specialities.pluck(:name).join(', ') %>">
    <% end %>
  </div>
</div>
