<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lembrete de Consulta</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #F59E0B;
      color: white;
      padding: 20px;
      text-align: center;
      border-radius: 8px 8px 0 0;
    }
    .content {
      background-color: #f8f9fa;
      padding: 30px;
      border-radius: 0 0 8px 8px;
    }
    .appointment-details {
      background-color: white;
      padding: 20px;
      border-radius: 8px;
      margin: 20px 0;
      border: 1px solid #e5e7eb;
    }
    .detail-row {
      display: flex;
      justify-content: space-between;
      padding: 8px 0;
      border-bottom: 1px solid #f3f4f6;
    }
    .detail-row:last-child {
      border-bottom: none;
    }
    .detail-label {
      font-weight: bold;
      color: #374151;
    }
    .detail-value {
      color: #6B7280;
    }
    .reminder-type {
      background-color: #FEF3C7;
      padding: 15px;
      border-radius: 8px;
      margin: 20px 0;
      border-left: 4px solid #F59E0B;
    }
    .footer {
      text-align: center;
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #ddd;
      color: #666;
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>🔔 Lembrete de Consulta</h1>
    <p>Não esqueça da sua consulta!</p>
  </div>
  
  <div class="content">
    <div class="reminder-type">
      <strong>⏰ Lembrete:</strong> 
      <% case @notification.metadata['reminder_type'] %>
      <% when '48h' %>
        Sua consulta é depois de amanhã
      <% when '24h' %>
        Sua consulta é amanhã
      <% when '2h' %>
        Sua consulta é em 2 horas
      <% end %>
    </div>
    
    <h2>Detalhes da Consulta</h2>
    
    <div class="appointment-details">
      <div class="detail-row">
        <span class="detail-label">Data:</span>
        <span class="detail-value"><%= @appointment.scheduled_at.strftime('%d/%m/%Y') %></span>
      </div>
      <div class="detail-row">
        <span class="detail-label">Horário:</span>
        <span class="detail-value"><%= @appointment.scheduled_at.strftime('%H:%M') %></span>
      </div>
      <div class="detail-row">
        <span class="detail-label">Profissional:</span>
        <span class="detail-value"><%= @appointment.professional.full_name %></span>
      </div>
      <div class="detail-row">
        <span class="detail-label">Agenda:</span>
        <span class="detail-value"><%= @appointment.agenda.name %></span>
      </div>
      <div class="detail-row">
        <span class="detail-label">Tipo:</span>
        <span class="detail-value"><%= @appointment.appointment_type.humanize %></span>
      </div>
    </div>
    
    <div style="background-color: #FEF3C7; padding: 15px; border-radius: 8px; margin: 20px 0;">
      <strong>📋 Importante:</strong> 
      <% case @notification.metadata['reminder_type'] %>
      <% when '48h' %>
        Confirme sua presença e prepare-se para a consulta.
      <% when '24h' %>
        Chegue com 15 minutos de antecedência.
      <% when '2h' %>
        Sua consulta está próxima! Chegue com antecedência.
      <% end %>
    </div>
    
    <p style="margin-top: 30px;">
      <a href="<%= root_url %>" style="background-color: #F59E0B; color: white; padding: 12px 24px; text-decoration: none; border-radius: 4px; display: inline-block;">
        Acessar Sistema
      </a>
    </p>
  </div>
  
  <div class="footer">
    <p>Esta é uma notificação automática do sistema IntegrarPlus.</p>
    <p>Se você não deseja receber estas notificações, entre em contato com o administrador.</p>
  </div>
</body>
</html>
