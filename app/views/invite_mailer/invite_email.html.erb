<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="color-scheme" content="light dark">
  <meta name="supported-color-schemes" content="light dark">
  <title>Convite IntegrarPlus</title>
  <style>
    /* Reset e configurações base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Inter', 'Segoe UI', Arial, sans-serif;
      font-size: 16px;
      line-height: 24px;
      color: #111827;
      background-color: #F6F8FB;
      margin: 0;
      padding: 0;
      -webkit-text-size-adjust: 100%;
      -ms-text-size-adjust: 100%;
    }
    
    /* Container principal - responsivo */
    .email-container {
      width: 100%;
      max-width: 100%;
      margin: 0;
      background-color: #FFFFFF;
      overflow: hidden;
    }
    
    /* Header responsivo */
    .header {
      background: linear-gradient(135deg, #3B82F6 0%, #2563EB 100%);
      padding: 32px 24px;
      text-align: center;
      position: relative;
      width: 100%;
    }
    
    .logo {
      width: 80px;
      height: 80px;
      margin: 0 auto 20px;
      display: block;
    }
    
    .logo img {
      width: 100%;
      height: 100%;
      object-fit: contain;
      border-radius: 12px;
    }
    
    .header h1 {
      color: white;
      font-size: 28px;
      font-weight: 600;
      margin-bottom: 12px;
    }
    
    .header p {
      color: rgba(255, 255, 255, 0.9);
      font-size: 18px;
      font-weight: 400;
    }
    
    /* Conteúdo principal - responsivo */
    .content {
      padding: 32px 24px;
      background-color: #FFFFFF;
      width: 100%;
    }
    
    .greeting {
      margin-bottom: 32px;
      text-align: center;
    }
    
    .greeting h2 {
      font-size: 24px;
      font-weight: 600;
      color: #111827;
      margin-bottom: 12px;
    }
    
    .greeting p {
      color: #374151;
      font-size: 18px;
    }
    
    /* Cartão de informações - responsivo */
    .info-card {
      background-color: #F8FAFC;
      border: 1px solid #E2E8F0;
      border-radius: 12px;
      padding: 24px;
      margin: 32px 0;
      width: 100%;
    }
    
    .info-card h3 {
      font-size: 18px;
      font-weight: 600;
      color: #111827;
      margin-bottom: 20px;
      text-align: center;
    }
    
    .info-list {
      list-style: none;
      display: grid;
      gap: 16px;
    }
    
    .info-list li {
      display: flex;
      align-items: center;
      color: #374151;
      padding: 12px;
      background-color: #FFFFFF;
      border-radius: 8px;
      border: 1px solid #E5E7EB;
    }
    
    .info-label {
      font-weight: 600;
      min-width: 100px;
      margin-right: 16px;
      color: #374151;
    }
    
    /* Callout de aviso - responsivo */
    .warning-callout {
      background-color: #FEF3C7;
      border: 1px solid #F59E0B;
      border-radius: 12px;
      padding: 20px;
      margin: 32px 0;
      display: flex;
      align-items: flex-start;
      gap: 16px;
      width: 100%;
    }
    
    .warning-icon {
      font-size: 24px;
      flex-shrink: 0;
      margin-top: 4px;
    }
    
    .warning-content h4 {
      font-size: 18px;
      font-weight: 600;
      color: #92400E;
      margin-bottom: 12px;
    }
    
    .warning-content ul {
      list-style: none;
      color: #92400E;
    }
    
    .warning-content li {
      margin-bottom: 8px;
      font-size: 16px;
    }
    
    /* Botão principal - responsivo */
    .cta-section {
      text-align: center;
      margin: 40px 0;
      width: 100%;
    }
    
    .cta-button {
      display: inline-block;
      background: linear-gradient(135deg, #3B82F6 0%, #2563EB 100%);
      color: white;
      text-decoration: none;
      padding: 20px 40px;
      border-radius: 12px;
      font-weight: 600;
      font-size: 18px;
      min-height: 56px;
      line-height: 1.2;
      box-shadow: 0 4px 6px -1px rgba(59, 130, 246, 0.3);
      transition: all 0.2s ease;
      width: 100%;
      max-width: 400px;
    }
    
    .cta-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 12px -1px rgba(59, 130, 246, 0.4);
    }
    
    /* Link alternativo - responsivo */
    .alternative-link {
      background-color: #F3F4F6;
      border: 1px solid #D1D5DB;
      border-radius: 12px;
      padding: 20px;
      margin: 32px 0;
      text-align: center;
      width: 100%;
    }
    
    .alternative-link p {
      color: #374151;
      font-size: 16px;
      margin-bottom: 12px;
    }
    
    .link-text {
      font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
      font-size: 14px;
      color: #2563EB;
      word-break: break-all;
      background-color: #FFFFFF;
      padding: 12px 16px;
      border-radius: 8px;
      border: 1px solid #E5E7EB;
      display: block;
      margin: 0 auto;
      max-width: 100%;
    }
    
    /* Rodapé - responsivo */
    .footer {
      background-color: #F9FAFB;
      border-top: 1px solid #E5E7EB;
      padding: 32px 24px;
      text-align: center;
      width: 100%;
    }
    
    .footer p {
      color: #6B7280;
      font-size: 16px;
      margin-bottom: 12px;
    }
    
    .footer p:last-child {
      margin-bottom: 0;
    }
    
    .company-info {
      margin-top: 20px;
      padding-top: 20px;
      border-top: 1px solid #E5E7EB;
    }
    
    /* Responsividade para telas maiores */
    @media only screen and (min-width: 768px) {
      .email-container {
        max-width: 800px;
        margin: 0 auto;
        border-radius: 16px;
        box-shadow: 0 8px 16px -1px rgba(0, 0, 0, 0.1);
      }
      
      .header {
        padding: 40px 32px;
      }
      
      .content {
        padding: 40px 32px;
      }
      
      .cta-button {
        width: auto;
        min-width: 300px;
      }
      
      .info-list {
        grid-template-columns: 1fr;
      }
    }
    
    /* Responsividade para telas muito pequenas */
    @media only screen and (max-width: 480px) {
      .header, .content, .footer {
        padding: 20px 16px;
      }
      
      .header h1 {
        font-size: 24px;
      }
      
      .greeting h2 {
        font-size: 20px;
      }
      
      .cta-button {
        padding: 16px 24px;
        font-size: 16px;
      }
    }
    
    /* Dark mode support */
    @media (prefers-color-scheme: dark) {
      body {
        background-color: #1F2937;
        color: #F9FAFB;
      }
      
      .email-container {
        background-color: #374151;
      }
      
      .content {
        background-color: #374151;
      }
      
      .info-card {
        background-color: #4B5563;
        border-color: #6B7280;
      }
      
      .info-list li {
        background-color: #374151;
        border-color: #6B7280;
      }
      
      .warning-callout {
        background-color: #78350F;
        border-color: #F59E0B;
      }
      
      .warning-content h4,
      .warning-content li {
        color: #FEF3C7;
      }
      
      .alternative-link {
        background-color: #4B5563;
        border-color: #6B7280;
      }
      
      .link-text {
        background-color: #374151;
        border-color: #6B7280;
        color: #60A5FA;
      }
      
      .footer {
        background-color: #4B5563;
        border-color: #6B7280;
      }
    }
  </style>
</head>
<body>
  <div class="email-container">
    <!-- Header responsivo -->
    <div class="header">
      <div class="logo">
        <img src="<%= asset_path('logo.png') %>" alt="IntegrarPlus Logo" />
      </div>
      <h1>IntegrarPlus</h1>
      <p>Convite para Acesso ao Sistema</p>
    </div>

    <!-- Conteúdo principal responsivo -->
    <div class="content">
      <div class="greeting">
        <h2>Olá <%= @professional&.full_name || @user.email %>!</h2>
        <p>Você foi convidado para acessar o sistema <strong>IntegrarPlus</strong>.</p>
      </div>

      <!-- Cartão de informações responsivo -->
      <div class="info-card">
        <h3>📋 Informações do seu acesso</h3>
        <ul class="info-list">
          <li>
            <span class="info-label">E-mail:</span>
            <span><%= @user.email %></span>
          </li>
          <% if @professional %>
            <li>
              <span class="info-label">Nome:</span>
              <span><%= @professional.full_name %></span>
            </li>
            <% if @professional.specialities.any? %>
              <li>
                <span class="info-label">Função:</span>
                <span><%= @professional.specialities.map(&:name).join(', ') %></span>
              </li>
            <% end %>
          <% end %>
        </ul>
      </div>

      <!-- Callout de aviso responsivo -->
      <div class="warning-callout">
        <div class="warning-icon">⚠️</div>
        <div class="warning-content">
          <h4>Importante</h4>
          <ul>
            <li>Este link é válido por 7 dias</li>
            <li>Após definir sua senha, você poderá acessar o sistema normalmente</li>
            <li>Se você não solicitou este convite, ignore este e-mail</li>
          </ul>
        </div>
      </div>

      <!-- Botão de ação responsivo -->
      <div class="cta-section">
        <a href="<%= @invite.invite_url %>" class="cta-button">
          Definir Senha e Acessar Sistema
        </a>
      </div>

      <!-- Link alternativo responsivo -->
      <div class="alternative-link">
        <p><strong>Se o botão não funcionar, copie e cole este link no seu navegador:</strong></p>
        <div class="link-text"><%= @invite.invite_url %></div>
      </div>
    </div>

    <!-- Rodapé responsivo -->
    <div class="footer">
      <p>Este é um e-mail automático do sistema IntegrarPlus.</p>
      <p>Você recebeu este convite porque foi adicionado ao sistema por um administrador.</p>
      <div class="company-info">
        <p><strong>IntegrarPlus</strong> - Sistema de Gestão Integrada</p>
        <p>Para suporte técnico, entre em contato com o administrador do sistema.</p>
      </div>
    </div>
  </div>
</body>
</html>
