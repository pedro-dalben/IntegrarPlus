DATABASE_USERNAME=your_db_user
DATABASE_PASSWORD=your_db_password
ASDF_SILENCE_WARNING=1
MEILISEARCH_HOST=http://localhost:7700
MEILISEARCH_API_KEY=your_meilisearch_key

# === Oracle Email Delivery (SMTP) ===
OCI_SMTP_HOST="smtp.email.sa-saopaulo-1.oci.oraclecloud.com"
OCI_SMTP_PORT="587"
# SMTP username gerado no IAM → SMTP credentials
OCI_SMTP_USERNAME="your_oci_smtp_username"
OCI_SMTP_PASSWORD="your_oci_smtp_password"

# === Domínio / remetente ===
MAIL_DOMAIN="integrarplus.com.br"
MAIL_FROM="contato@integrarplus.com.br"   # Approved Sender na mesma região

# === Host da aplicação (URLs em e-mails, links de reset, etc.) ===
# Desenvolvimento:
APP_HOST="localhost:3001"                 # localhost:3001 para dev
APP_PROTOCOL="http"                       # http para dev
# Produção:
# APP_HOST="app.integrarplus.com.br"      # ajuste para o host real da app
# APP_PROTOCOL="https"                    # https para produção
SENTRY_DSN=""
